{
  "name": "allin-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "party": "partykit dev",
    "party:deploy": "partykit deploy"
  },
  "dependencies": {
    "partysocket": "^1.0.3"
  },
  "devDependencies": {
    "partykit": "^0.0.114",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
